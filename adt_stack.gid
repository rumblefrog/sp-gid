¦ifunctionsŠ¿dnamekCreateStackhdocStartûfdocEndæddocs¢ebriefyÍCreates a stack structure.  A stack is a LIFO (last in, first out) vector (array) of items.  It has O(1) insertion and O(1) removal. 
 Stacks have two operations: Push (adding an item) and Pop (removes items in reverse-push order). 
 The contents of the stack are uniform; i.e. storing a string and then retrieving it as an integer is NOT the same as StringToInt()! 
 The "blocksize" determines how many cells each slot has; it cannot be changed after creation.dtagsƒ¢ctag`dtextyÍCreates a stack structure.  A stack is a LIFO (last in, first out) vector (array) of items.  It has O(1) insertion and O(1) removal. 
 Stacks have two operations: Push (adding an item) and Pop (removes items in reverse-push order). 
 The contents of the stack are uniform; i.e. storing a string and then retrieving it as an integer is NOT the same as StringToInt()! 
 The "blocksize" determines how many cells each slot has; it cannot be changed after creation.¢ctagoparam:blocksizedtexty?Creates a stack structure.  A stack is a LIFO (last in, first out) vector (array) of items.  It has O(1) insertion and O(1) removal. 
 Stacks have two operations: Push (adding an item) and Pop (removes items in reverse-push order). 
 The contents of the stack are uniform; i.e. storing a string and then retrieving it as an integer is NOT the same as StringToInt()! 
 The "blocksize" determines how many cells each slot has; it cannot be changed after creation. 
 The number of cells each entry in the stack can hold.  For example, 32 cells is equivalent to: new Array[X][32]¢ctagfreturndtextyQCreates a stack structure.  A stack is a LIFO (last in, first out) vector (array) of items.  It has O(1) insertion and O(1) removal. 
 Stacks have two operations: Push (adding an item) and Pop (removes items in reverse-push order). 
 The contents of the stack are uniform; i.e. storing a string and then retrieving it as an integer is NOT the same as StringToInt()! 
 The "blocksize" determines how many cells each slot has; it cannot be changed after creation. 
 The number of cells each entry in the stack can hold.  For example, 32 cells is equivalent to: new Array[X][32] New stack Handle.dkindfnativejreturnTypejArrayStackiarguments£dtypecintdnameiblocksizeddeclmint blocksizeÿ¿dnamemPushStackCellhdocStartfdocEndHddocs¢ebriefx‰Pushes a value onto the end of the stack, adding a new index. 
 This may safely be used even if the stack has a blocksize greater than 1.dtags„¢ctag`dtextx‰Pushes a value onto the end of the stack, adding a new index. 
 This may safely be used even if the stack has a blocksize greater than 1.¢ctagkparam:stackdtextx™Pushes a value onto the end of the stack, adding a new index. 
 This may safely be used even if the stack has a blocksize greater than 1. 
 Stack Handle.¢ctagkparam:valuedtextx¨Pushes a value onto the end of the stack, adding a new index. 
 This may safely be used even if the stack has a blocksize greater than 1. 
 Stack Handle. Value to push.¢ctageerrordtextxÉPushes a value onto the end of the stack, adding a new index. 
 This may safely be used even if the stack has a blocksize greater than 1. 
 Stack Handle. Value to push. Invalid Handle or out of memory.dkindfnativejreturnTypedvoidiarguments‚£dtypefHandlednameestackddecllHandle stack£dtypecanydnameevalueddecliany valueÿ¿dnameoPushStackStringhdocStartfdocEndsddocs¢ebriefxRPushes a copy of a string onto the end of a stack, truncating it if it is too big.dtags„¢ctag`dtextxRPushes a copy of a string onto the end of a stack, truncating it if it is too big.¢ctagkparam:stackdtextxbPushes a copy of a string onto the end of a stack, truncating it if it is too big. 
 Stack Handle.¢ctagkparam:valuedtextxrPushes a copy of a string onto the end of a stack, truncating it if it is too big. 
 Stack Handle. String to push.¢ctageerrordtextx“Pushes a copy of a string onto the end of a stack, truncating it if it is too big. 
 Stack Handle. String to push. Invalid Handle or out of memory.dkindfnativejreturnTypedvoidiarguments‚£dtypefHandlednameestackddecllHandle stack£dtypelconst char[]dnameevalueddeclrconst char[] valueÿ¿dnamenPushStackArrayhdocStart·fdocEndddocs¢ebriefx¹Pushes a copy of an array of cells onto the end of a stack.  The cells are pushed as a block (i.e. the entire array takes up one stack slot), rather than pushing each cell individually.dtags…¢ctag`dtextx¹Pushes a copy of an array of cells onto the end of a stack.  The cells are pushed as a block (i.e. the entire array takes up one stack slot), rather than pushing each cell individually.¢ctagkparam:stackdtextxÉPushes a copy of an array of cells onto the end of a stack.  The cells are pushed as a block (i.e. the entire array takes up one stack slot), rather than pushing each cell individually. 
 Stack Handle.¢ctaglparam:valuesdtextxâPushes a copy of an array of cells onto the end of a stack.  The cells are pushed as a block (i.e. the entire array takes up one stack slot), rather than pushing each cell individually. 
 Stack Handle. Block of values to copy.¢ctagjparam:sizedtextyPushes a copy of an array of cells onto the end of a stack.  The cells are pushed as a block (i.e. the entire array takes up one stack slot), rather than pushing each cell individually. 
 Stack Handle. Block of values to copy. If not set, the number of elements copied from the array will be equal to the blocksize.  If set higher than the blocksize, the operation will be truncated.¢ctageerrordtexty Pushes a copy of an array of cells onto the end of a stack.  The cells are pushed as a block (i.e. the entire array takes up one stack slot), rather than pushing each cell individually. 
 Stack Handle. Block of values to copy. If not set, the number of elements copied from the array will be equal to the blocksize.  If set higher than the blocksize, the operation will be truncated. Invalid Handle or out of memory.dkindfnativejreturnTypedvoidiargumentsƒ£dtypefHandlednameestackddecllHandle stack£dtypekconst any[]dnamefvaluesddeclrconst any[] values£dtypecintdnamedsizeddeclhint sizeÿ¿dnamelPopStackCellhdocStartUfdocEndddocs¢ebriefxPops a cell value from a stack.dtags‡¢ctag`dtextxPops a cell value from a stack.¢ctagkparam:stackdtextx/Pops a cell value from a stack. 
 Stack Handle.¢ctagkparam:valuedtextxLPops a cell value from a stack. 
 Stack Handle. Variable to store the value.¢ctagkparam:blockdtextx—Pops a cell value from a stack. 
 Stack Handle. Variable to store the value. Optionally specify which block to read from (useful if the blocksize > 0).¢ctaglparam:asChardtextxÄPops a cell value from a stack. 
 Stack Handle. Variable to store the value. Optionally specify which block to read from (useful if the blocksize > 0). Optionally read as a byte instead of a cell.¢ctagfreturndtextxòPops a cell value from a stack. 
 Stack Handle. Variable to store the value. Optionally specify which block to read from (useful if the blocksize > 0). Optionally read as a byte instead of a cell. True on success, false if the stack is empty.¢ctageerrordtextyPops a cell value from a stack. 
 Stack Handle. Variable to store the value. Optionally specify which block to read from (useful if the blocksize > 0). Optionally read as a byte instead of a cell. True on success, false if the stack is empty. Invalid Handle.dkindfnativejreturnTypedbooliarguments„£dtypefHandlednameestackddecllHandle stack£dtypedany&dnameevalueddecljany& value£dtypecintdnameeblockddecliint block£dtypedbooldnamefasCharddeclkbool asCharÿ¿dnamenPopStackStringhdocStartnfdocEndddocs¢ebriefx!Pops a string value from a stack.dtags†¢ctag`dtextx!Pops a string value from a stack.¢ctagkparam:stackdtextx1Pops a string value from a stack. 
 Stack Handle.¢ctaglparam:bufferdtextxIPops a string value from a stack. 
 Stack Handle. Buffer to store string.¢ctagoparam:maxlengthdtextxePops a string value from a stack. 
 Stack Handle. Buffer to store string. Maximum size of the buffer.¢ctagfreturndtextx“Pops a string value from a stack. 
 Stack Handle. Buffer to store string. Maximum size of the buffer. True on success, false if the stack is empty.¢ctageerrordtextx£Pops a string value from a stack. 
 Stack Handle. Buffer to store string. Maximum size of the buffer. True on success, false if the stack is empty. Invalid Handle.dkindfnativejreturnTypedbooliarguments„£dtypefHandlednameestackddecllHandle stack£dtypefchar[]dnamefbufferddeclmchar[] buffer£dtypecintdnameimaxlengthddeclmint maxlength£dtypedint&dnamegwrittenddecllint& writtenÿ¿dnamemPopStackArrayhdocStartúfdocEnd “ddocs¢ebriefx$Pops an array of cells from a stack.dtags†¢ctag`dtextx$Pops an array of cells from a stack.¢ctagkparam:stackdtextx4Pops an array of cells from a stack. 
 Stack Handle.¢ctaglparam:bufferdtextxRPops an array of cells from a stack. 
 Stack Handle. Buffer to store the array in.¢ctagjparam:sizedtextx¶Pops an array of cells from a stack. 
 Stack Handle. Buffer to store the array in. If not set, assumes the buffer size is equal to the blocksize.  Otherwise, the size passed is used.¢ctagfreturndtextxäPops an array of cells from a stack. 
 Stack Handle. Buffer to store the array in. If not set, assumes the buffer size is equal to the blocksize.  Otherwise, the size passed is used. True on success, false if the stack is empty.¢ctageerrordtextxôPops an array of cells from a stack. 
 Stack Handle. Buffer to store the array in. If not set, assumes the buffer size is equal to the blocksize.  Otherwise, the size passed is used. True on success, false if the stack is empty. Invalid Handle.dkindfnativejreturnTypedbooliargumentsƒ£dtypefHandlednameestackddecllHandle stack£dtypeeany[]dnamefbufferddecllany[] buffer£dtypecintdnamedsizeddeclhint sizeÿ¿dnamelIsStackEmptyhdocStart ÜfdocEnd!”ddocs¢ebriefxChecks if a stack is empty.dtags„¢ctag`dtextxChecks if a stack is empty.¢ctagkparam:stackdtextx+Checks if a stack is empty. 
 Stack Handle.¢ctagfreturndtextxNChecks if a stack is empty. 
 Stack Handle. True if empty, false if not empty.¢ctageerrordtextx^Checks if a stack is empty. 
 Stack Handle. True if empty, false if not empty. Invalid Handle.dkindfnativejreturnTypedbooliarguments£dtypefHandlednameestackddecllHandle stackÿ¿dnamehPopStackhdocStart!ÁfdocEnd"›ddocs¢ebriefx1Pops a value off a stack, ignoring it completely.dtags„¢ctag`dtextx1Pops a value off a stack, ignoring it completely.¢ctagkparam:stackdtextxAPops a value off a stack, ignoring it completely. 
 Stack Handle.¢ctagfreturndtextxpPops a value off a stack, ignoring it completely. 
 Stack Handle. True if something was popped, false otherwise.¢ctageerrordtextx€Pops a value off a stack, ignoring it completely. 
 Stack Handle. True if something was popped, false otherwise. Invalid Handle.dkindestockjreturnTypedbooliarguments£dtypefHandlednameestackddecllHandle stackÿ¿dnameqGetStackBlockSizehdocStart"úfdocEnd#Àddocs¢ebriefx1Returns the blocksize the stack was created with.dtags„¢ctag`dtextx1Returns the blocksize the stack was created with.¢ctagkparam:stackdtextxAReturns the blocksize the stack was created with. 
 Stack Handle.¢ctagfreturndtextx]Returns the blocksize the stack was created with. 
 Stack Handle. The blocksize of the stack.¢ctageerrordtextxlReturns the blocksize the stack was created with. 
 Stack Handle. The blocksize of the stack. Invalid HandledkindfnativejreturnTypecintiarguments£dtypefHandlednameestackddecllHandle stackÿjmethodmaps¿dnamejArrayStackhdocStart fdocEnd ddocsögmethods‡¿dnamejArrayStackhdocStart fdocEnd	addocs¢ebriefyÍCreates a stack structure.  A stack is a LIFO (last in, first out) vector (array) of items.  It has O(1) insertion and O(1) removal. 
 Stacks have two operations: Push (adding an item) and Pop (removes items in reverse-push order). 
 The contents of the stack are uniform; i.e. storing a string and then retrieving it as an integer is NOT the same as StringToInt()! 
 The "blocksize" determines how many cells each slot has; it cannot be changed after creation.dtags‚¢ctag`dtextyÍCreates a stack structure.  A stack is a LIFO (last in, first out) vector (array) of items.  It has O(1) insertion and O(1) removal. 
 Stacks have two operations: Push (adding an item) and Pop (removes items in reverse-push order). 
 The contents of the stack are uniform; i.e. storing a string and then retrieving it as an integer is NOT the same as StringToInt()! 
 The "blocksize" determines how many cells each slot has; it cannot be changed after creation.¢ctagoparam:blocksizedtexty>Creates a stack structure.  A stack is a LIFO (last in, first out) vector (array) of items.  It has O(1) insertion and O(1) removal. 
 Stacks have two operations: Push (adding an item) and Pop (removes items in reverse-push order). 
 The contents of the stack are uniform; i.e. storing a string and then retrieving it as an integer is NOT the same as StringToInt()! 
 The "blocksize" determines how many cells each slot has; it cannot be changed after creation. 
 The number of cells each entry in the stack can hold.  For example, 32 cells is equivalent to: new Array[X][32dkindöjreturnTypejArrayStackiarguments£dtypecintdnameiblocksizeddeclmint blocksizeÿ¿dnamedPushhdocStart	“fdocEnd
Yddocs¢ebriefx‰Pushes a value onto the end of the stack, adding a new index. 
 This may safely be used even if the stack has a blocksize greater than 1.dtags‚¢ctag`dtextx‰Pushes a value onto the end of the stack, adding a new index. 
 This may safely be used even if the stack has a blocksize greater than 1.¢ctagkparam:valuedtextx™Pushes a value onto the end of the stack, adding a new index. 
 This may safely be used even if the stack has a blocksize greater than 1. 
 Value to pushdkindöjreturnTypedvoidiarguments£dtypecanydnameevalueddecliany valueÿ¿dnamejPushStringhdocStart
„fdocEndddocs¢ebriefxRPushes a copy of a string onto the end of a stack, truncating it if it is too big.dtags‚¢ctag`dtextxRPushes a copy of a string onto the end of a stack, truncating it if it is too big.¢ctagkparam:valuedtextxcPushes a copy of a string onto the end of a stack, truncating it if it is too big. 
 String to pushdkindöjreturnTypedvoidiarguments£dtypelconst char[]dnameevalueddeclrconst char[] valueÿ¿dnameiPushArrayhdocStartFfdocEndRddocs¢ebriefx¸Pushes a copy of an array of cells onto the end of a stack. The cells are pushed as a block (i.e. the entire array takes up one stack slot), rather than pushing each cell individually.dtags„¢ctag`dtextx¸Pushes a copy of an array of cells onto the end of a stack. The cells are pushed as a block (i.e. the entire array takes up one stack slot), rather than pushing each cell individually.¢ctagkparam:stackdtextxÈPushes a copy of an array of cells onto the end of a stack. The cells are pushed as a block (i.e. the entire array takes up one stack slot), rather than pushing each cell individually. 
 Stack Handle.¢ctaglparam:valuesdtextxáPushes a copy of an array of cells onto the end of a stack. The cells are pushed as a block (i.e. the entire array takes up one stack slot), rather than pushing each cell individually. 
 Stack Handle. Block of values to copy.¢ctagjparam:sizedtexty}Pushes a copy of an array of cells onto the end of a stack. The cells are pushed as a block (i.e. the entire array takes up one stack slot), rather than pushing each cell individually. 
 Stack Handle. Block of values to copy. If not set, the number of elements copied from the array will be equal to the blocksize.  If set higher than the blocksize, the operation will be truncateddkindöjreturnTypedvoidiarguments‚£dtypekconst any[]dnamefvaluesddeclrconst any[] values£dtypecintdnamedsizeddeclhint sizeÿ¿dnamecPophdocStart˜fdocEndåddocs¢ebriefxPops a cell value from a stack.dtags…¢ctag`dtextxPops a cell value from a stack.¢ctagkparam:blockdtextxlPops a cell value from a stack. 
 Optionally specify which block to read from (useful if the blocksize > 0).¢ctaglparam:asChardtextx™Pops a cell value from a stack. 
 Optionally specify which block to read from (useful if the blocksize > 0). Optionally read as a byte instead of a cell.¢ctagfreturndtextx¶Pops a cell value from a stack. 
 Optionally specify which block to read from (useful if the blocksize > 0). Optionally read as a byte instead of a cell. Value popped from the stack.¢ctageerrordtextxÉPops a cell value from a stack. 
 Optionally specify which block to read from (useful if the blocksize > 0). Optionally read as a byte instead of a cell. Value popped from the stack. The stack is emptydkindöjreturnTypecanyiarguments‚£dtypecintdnameeblockddecliint block£dtypedbooldnamefasCharddeclkbool asCharÿ¿dnameiPopStringhdocStart#fdocEnd\ddocs¢ebriefx!Pops a string value from a stack.dtags…¢ctag`dtextx!Pops a string value from a stack.¢ctaglparam:bufferdtextx;Pops a string value from a stack. 
 Buffer to store string.¢ctagoparam:maxlengthdtextxWPops a string value from a stack. 
 Buffer to store string. Maximum size of the buffer.¢ctagmparam:writtendtextx¢Pops a string value from a stack. 
 Buffer to store string. Maximum size of the buffer. Number of characters written to buffer, not including the null terminator.¢ctageerrordtextxµPops a string value from a stack. 
 Buffer to store string. Maximum size of the buffer. Number of characters written to buffer, not including the null terminator. The stack is emptydkindöjreturnTypedvoidiargumentsƒ£dtypefchar[]dnamefbufferddeclmchar[] buffer£dtypecintdnameimaxlengthddeclmint maxlength£dtypedint&dnamegwrittenddecllint& writtenÿ¿dnamehPopArrayhdocStart±fdocEnd×ddocs¢ebriefx$Pops an array of cells from a stack.dtags„¢ctag`dtextx$Pops an array of cells from a stack.¢ctaglparam:bufferdtextxDPops an array of cells from a stack. 
 Buffer to store the array in.¢ctagjparam:sizedtextx¨Pops an array of cells from a stack. 
 Buffer to store the array in. If not set, assumes the buffer size is equal to the blocksize.  Otherwise, the size passed is used.¢ctageerrordtextx»Pops an array of cells from a stack. 
 Buffer to store the array in. If not set, assumes the buffer size is equal to the blocksize.  Otherwise, the size passed is used. The stack is emptydkindöjreturnTypedvoidiarguments‚£dtypeeany[]dnamefbufferddecllany[] buffer£dtypecintdnamedsizeddeclhint sizeÿjproperties‚¿dnameeEmptyhdocStartfdocEndMddocs¢ebriefx3Returns true if the stack is empty, false otherwisedtags¢ctag`dtextx3Returns true if the stack is empty, false otherwisedtypedboolfgetterõfsetterôÿ¿dnameiBlockSizehdocStart‡fdocEnd¼ddocs¢ebriefx1Retrieve the blocksize the stack was created withdtags¢ctag`dtextx1Retrieve the blocksize the stack was created withdtypecintfgetterõfsetterôÿÿiconstants€eenums€htypesets€htypedefs€